'use strict'

// import { Pose } from '../Pose.js'

export class ClientAddPose {
  /**
   * @param {Client} client
   * @param {object} data
   * @returns {boolean}
   */
  static run (client, data) {
    // TODO

    return false
  }
}
