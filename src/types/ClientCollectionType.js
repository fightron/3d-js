/**
 * A Map of other Maps.
 * The first map is indexed by a shorthand string - the constants from ClientCollections.
 * The second map is indexed by a string or a number.
 *
 * @typedef {Map<string,Map<number|string,Animation3D|Camera|Geometry|Light|Material|Mesh|Pose|Skeleton|Sound>>} ClientCollectionType
 */
